introspection:
  - "Has the search functionality changed? Do we need to render a different UI for search?"
  - "Does the create functionality need to change? Are new fields added to the create form? Are new actions added to the create form?"
  - "Does the edit functionality need to change? Are new fields added to the edit form? Are new actions added to the edit form?"
  - "Has the view functionality changed? Do we need to render a different UI for view?"
  - "Do we need to get new fields from the service layer on fetching the item?"
