up:
  - "Did the API change? Did we add new queries or mutations? Did the fields or types change?"
  - "Did the model change? Did we add new fields or remove existing fields?"
down:
  - "Did the card change? if so has the changes been reflected in the bot code that binds data with the card template?"
