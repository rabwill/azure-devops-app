introspection:
  - "Does the Search API query need to be updated? New fields required? Or existing fields removed?"
  - "Do we need to change query of Azure DevOps? New fields required? Or existing fields removed?"
